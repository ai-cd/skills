apiVersion: v1
kind: Service
metadata:
  name: app-name-svc
  labels:
    app: app-name
    managed-by: sealos-skills
spec:
  type: ClusterIP
  selector:
    app: app-name
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP